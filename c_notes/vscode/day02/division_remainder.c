#include <stdio.h>

//
int main()
{
    int dividend, divisor, quotient, remainder;
    scanf("%d%d", &dividend, &divisor);
    printf("%d", dividend/divisor);
    printf(" %d", dividend%divisor);
    return 0;
}
